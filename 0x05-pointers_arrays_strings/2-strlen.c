#include "main.h"
#include "string.h"
#include <stdio.h>
/**
 * _strlen - Returns length of a string
 * @s: string to return length
 * Return: 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}

