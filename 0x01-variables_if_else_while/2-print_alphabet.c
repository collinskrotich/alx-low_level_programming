#include <stdio.h>

/**
 * main - prints if number is positive, negative or 0
 * Return: Always 0
 */
int main(void) /* main function */
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
	putchar('\n');
	return (0);
}
