#include "main.h"
#include <string.h>
/**
 *_strlen_recursion - Return string length
 *@s: string to return length
 *Return: length
 */
int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
