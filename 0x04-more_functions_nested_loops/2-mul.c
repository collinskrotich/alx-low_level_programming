#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: Integer to multiply with
 * @b: Integer to multiply
 * Return: result
 */

int mul(int a, int b)
{
	int result = 0;

	result = a * b;
	return (result);
	putchar('\n');
}

